# WDR5 Connect - Eleventy version

This version makes Decap CMS posts appear automatically on the Updates page.

Netlify settings:
- Build command: npm run build
- Publish directory: _site

Decap CMS:
- Admin page: /admin/
- New updates are saved in /updates as Markdown files
- Netlify rebuilds the site after each published update
